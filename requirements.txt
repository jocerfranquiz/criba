# Runtime dependencies. pypdfium2 pinned to v5: the code uses the v5-era
# helper API (PdfImage/PdfTextObj(obj.raw, ...), get_objects(filter=...)).
pypdfium2>=5,<6
Pillow>=9.0
# powers extract(validate=True) and schema.OUTPUT_SCHEMA validation
jsonschema>=4.0
